# training model in the local machine

* Make sure Python is installed:
```
python --version
```

* install jupyter
```
pip install notebook
jupyter notebook
```

* install requirements.txt 
```
pip install -r requirements.txt
```